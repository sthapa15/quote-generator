import React from "react";
import ReactDOM from "react-dom"; //hook onto element that our whole app is contained in
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
